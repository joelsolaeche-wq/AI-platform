# /app

Next.js App Router pages and layouts.

This directory holds the route tree for the web frontend, following the
Next.js App Router convention (`page.tsx`, `layout.tsx`, `loading.tsx`, etc.).

> **Note:** The primary web application currently lives under `web/app/`.
> This root-level folder is reserved for a future migration or colocation.

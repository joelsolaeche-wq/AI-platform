# /components

Shared React UI components.

Reusable presentational and layout components used across the web frontend.
Follows the convention of co-locating component files with their styles and tests.

> **Note:** The primary component library currently lives under `web/components/`.
> This root-level folder is reserved for a future migration or colocation.

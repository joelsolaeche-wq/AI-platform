# /lib

Shared utility libraries and helpers.

Framework-agnostic utilities, client SDKs, configuration helpers, and
shared logic used across the frontend and other packages.

> **Note:** Frontend-specific utilities currently live under `web/lib/`.
> This root-level folder is for cross-cutting shared code.
